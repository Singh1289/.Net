                             
echo -n "no of rows : "
read n
for (( i=0 ; i<$n ; i++ ))
do

 for (( j=1 ; j<=i+1 ; j++ ))
  do
        echo -n "*"
  done
  echo
done

for (( k=$n-1 ;k>=1 ; k-- ))
do
   for (( m=1 ; m<=k ; m++ ))
   do
       echo -n "*"
  done
  echo
done
